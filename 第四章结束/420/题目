题目：
    假设iter的类型是vector<string>::iterator
    说明下面的表达式是否合法，如果合法，表达式
    的含义是什么？如果不合法，改写


表达式
    (1)*iter++;
    (2)（*iter)++;
    (3)iter->empty()
    (4)++*iter;


解答：
    （1）合法
    后置++优先与*
    所以是解引用当前迭代器
    然后再把当前迭代器的位置向后移动
    (2)非法
    先吧迭代器接引用，然后对迭代器指向的内容进行++；
    string类没有定义string的++
    （3）合法；
    相当于一个string对象调用empty（）函数
    （4）非法
    同2
